> 此文档专注于模组的更新内容
>
> 提供链接说明模组对应的最低Wiki版本
>
> 没有提供链接的表示继承上一个模组版本适配的Wiki版本

## v0.1.1

最低Wiki版本：[Wiki_26_03_29_23_59](Wiki更新日志.md#wiki_26_03_29_23_59)

**数据格式变更**
* 移除物品模板中的 `$type` 字段（保留向后兼容读取）
* **类型机制澄清**：物品属性由实际赋值的 Info 字段决定，与 `baseInfo.type` 无关
* 数据文件现支持 JSONC 注释（单行 `//` 和多行 `/* */`）

**新功能**
* 支持中文稀有度键（普通、稀有、超级稀有、不存在）
* 支持中文物品音效键
* 支持弹药类型物品验证

**配置更新**
* 新增 `cacheInitialized` 配置项，控制缓存初始化跳过逻辑
* 新增 `alwaysUpdateCache` 配置项，强制更新缓存
* 新增 `requiredItemIds` 配置项，验证必需物品存在性与启用状态
* 详见[模块配置](../关于模组/模块配置.md)

**验证系统**
* 新增 BaseInfoValidator、AttributeInfoValidator、BuffsInfoValidator
* 新增 MedicalInfoValidator、DrinkFoodInfoValidator、AmmoInfoValidator
* 移除无效的负数重量校验逻辑

**架构优化**
* 将 UpdateProperties 和 UpdateDatabaseService 方法改为 protected，限制外部调用
* 增强 BaseInfo 和 BuffsInfo 的空值校验逻辑

## v0.1.0

最低Wiki版本：[Wiki_26_03_16_03_40](Wiki更新日志.md#wiki_26_03_16_03_40)

* 完全移除所有 WebUI Razor 文件，包括布局、页面及各类信息组件，原有的物品创建和管理界面将不可访问
* 移除内置 WebUI 服务，删除 HttpServer、WebApplicationBuilder 及 MudBlazor.Services 相关引用
* 移除废弃的 WebUI 访问指南和文档中相关操作说明，等待后续重构完成的新 UI 或检查替代方案
* 优化物品验证流程，统一参数命名，将属性生成控制在验证通过后执行，避免重复计算
* 增强 NewItemCommon 验证逻辑，当提供 BuffsInfo 但未提供 StimulatorBuffs 时静默清理数据并记录原因
* 优化物品加载日志格式，增加物品启用状态的动态显示，提升调试信息可读性
* 升级项目版本号从 0.0.4 至 0.1.0，体现本次重大重构
* 更新 config.json，新增 ignoreTemplateFiles: true 默认配置
* 移除原有的 JSON 缓存文件结构（ParentIdCache.json、AmmoCache.json 等），启动时会重新生成所需统计数据

**新缓存系统**

* 输出目录: `游戏根目录\SPT\user\mods\SptItemCreator\StatsCache`
* 内容: 模组根据当前SPT的所有数据库, 整理的一份按照类型分类好的, 每个类型有的属性名称与所有出现过的值的列表
* 用处: 创建新物品时的参考; 制作脚本统计频数, 数值分布, 均值等的基础

## v0.0.4

最低Wiki版本：[Wiki_26_02_02_18_20](Wiki更新日志.md#wiki_26_02_02_18_20) 

- 实现设置页面(SettingPage)，常驻导航栏，提供WebUI配置界面
- 优化DataLoader，支持根据配置动态忽略模板文件
- 将所有模板文件移动至`模组文件夹/data/模板-Template`文件夹下
- 将模板文件名改为中英双语, 避免部分翻译器结果与内容不对应
- 默认日志文件最大大小从10MB调整至512KB(启动模组时超过这个大小会重新创建日志文件)

## v0.0.3

最低Wiki版本：[Wiki_26_01_30_14_45](Wiki更新日志.md#wiki_26_01_30_14_45) 

- 新增WebUI用于创建物品
  - 增加BaseInfoWidget控件可视化修改BaseInfo数据
  - 增加AttributeInfoWidget控件可视化修改AttributeInfo数据
  - 增加DrinkFoodInfoWidget控件可视化修改DrinkFoodInfo数据
  - 增加MedicalInfoWidget控件可视化修改MedicalInfo数据
  - 增加BuffsInfoWidget控件可视化修改BuffsInfo数据
  - 增加PropertyOverrideWidget控件用于简单查看与修改PropertyOverride数据
  - 增加用于创建**通用物品**、**药品**、**食物与饮品**的页面(由于发现弹药还有许多相关属性, 暂时搁置)
  
- 当赋值有效的CloneId后ParentId改为可选赋值, 如果CloneId无效将会报错(也更新了Wiki教程中的表格)
- 新增缓存系统, 从SPT服务器缓存ParentId, BackgroundColor数据(记录缓存时间: OnLoadOrder.TraderCallbacks - 1)
- `AttributeInfo`新增`BackgroundColor`字段

## v0.0.2

最低Wiki版本：[Wiki_26_01_23_19_00](Wiki更新日志.md#wiki_26_01_23_19_00) 

- `baseInfo`新增`allowAll`, `canFilter`, `cantFilter`字段用于控制哪些容器允许放置本物品
- 补充模组元数据url信息
- 移除了`AbstractInfo`中未正确生效的静态属性`ShouldUpdateDatabaseService`
- 所有`AbstractInfo`类型均增加`ItemPath`属性, 方便错误输出
- 优化了反序列化文件时的代码风格
- 修复了`BuffsInfo`中的判断非空与错误信息输出的逻辑错误
- 修复了`测试-食物模板.sic.jsonc`和`测试-饮品模板.sic.jsonc`的`buffsInfo.stimulatorBuffs`赋了无意义空字符串的问题

已知问题

- 通过克隆创建的容器类新物品, 即使使用了`allowAll=true`, 也会和克隆的物品一样无法在其他不兼容容器内存放(此问题可以通过清理本地缓存解决, 有小概率需要多次清理缓存)

## v0.0.1

最低Wiki版本：[Wiki_26_01_23_12_33](Wiki更新日志.md#wiki_26_01_23_12_33) 

- 提供了通用(common)类型物品创建方式
- 提供了对急救包, 针剂, 弹药, **针剂, 食物或饮品效果**创建的封装
